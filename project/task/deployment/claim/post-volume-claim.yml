apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: post-volume-claim
  namespace: vdmtest
spec:
  storageClassName: post-local-volume-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi